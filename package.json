{
  "name": "superpack-snitch",
  "version": "0.0.8",
  "description": "BLOCK your agents from ClawHub & Others. Blocklist guard for OpenClaw — hard-blocks tool calls, injects security directives, and broadcasts Telegram alerts.",
  "type": "module",
  "license": "MIT",
  "author": "Rob Vella <me@robvella.com>",
  "repository": {
    "type": "git",
    "url": "https://github.com/rgr4y/superpack-snitch"
  },
  "scripts": {
    "lint": "eslint src hooks --ext .ts",
    "test": "node --experimental-strip-types --test test/snitch.test.ts",
    "postinstall": "node --experimental-strip-types bin/postinstall.ts",
    "build:skill": "rm -rf dist/skill && mkdir -p dist/skill && cp SKILL.md dist/skill/ && cp -r hooks dist/skill/hooks && cd dist/skill && zip -r ../superpack-snitch-skill.zip . && echo '✓ dist/superpack-snitch-skill.zip'",
    "version": "gsed -i \"s/^version: .*/version: $npm_package_version/\" SKILL.md && git add SKILL.md"
  },
  "bin": {
    "snitch-check": "./bin/snitch-check.ts"
  },
  "devDependencies": {
    "@typescript-eslint/eslint-plugin": "^8",
    "@typescript-eslint/parser": "^8",
    "eslint": "^9"
  },
  "keywords": [
    "openclaw",
    "security",
    "plugin",
    "blocklist",
    "guard"
  ],
  "openclaw": {
    "extensions": [
      "./src/index.ts"
    ]
  },
  "files": [
    "src/",
    "hooks/",
    "bin/postinstall.ts",
    "openclaw.plugin.json",
    "README.md",
    "CHANGELOG.md"
  ]
}
